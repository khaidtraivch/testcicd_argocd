# Khóa dự án trên SonarQube (bắt buộc, bạn đã có sẵn)
sonar.projectKey=khainx106-group_flaskbase_ci_deploy_5c3db64c-02ca-4df6-8921-db0e69eb5a0

# Tên hiển thị (tùy chọn)
sonar.projectName=flask-python-ci

# Phiên bản (tuỳ chọn, có thể dùng biến GitLab nếu build động)
sonar.projectVersion=1.0

# Thư mục chứa mã nguồn chính
sonar.sources=app

# Các file/dạng file muốn bỏ qua
sonar.exclusions=**/__pycache__/**, **/*.pyc, **/*.pyo, tests/**, venv/**

# Ngôn ngữ
sonar.language=py

# Encoding mặc định
sonar.sourceEncoding=UTF-8

# Quality Gate blocking (chờ kết quả trước khi tiếp tục pipeline)
sonar.qualitygate.wait=true

# Coverage report nếu bạn dùng pytest + coverage
# (chưa bắt buộc nếu chưa cần đo coverage)
# sonar.python.coverage.reportPaths=coverage.xml
