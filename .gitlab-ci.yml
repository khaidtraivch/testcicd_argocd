stages:
  - build
  - deploy

build:
  stage: build
  script:
    - echo "Build step: compiling, linting, testing, etc."

deploy:
  stage: deploy
  script:
    - echo "Deploy step: transferring artifacts or restarting service"
    # Không còn lệnh scp hoặc python liên quan đến send_email.py
